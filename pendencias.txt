PENDÊNCIAS — painel "avisos de reposição" / notificações automáticas
Atualizado em 09/09/2026

Ordem de trabalho combinada: design primeiro, funcionalidades depois.

===========================================
DESIGN (resolver primeiro)
===========================================

- [RESOLVIDO em 08/09/2026] Visual dos pills de canal quando mais de um
  está selecionado (ex.: Push + E-mail + WhatsApp marcados juntos): antes
  o estado "active" usava só sombra leve + cor do texto, hoje
  .pill.active (style.css) usa preenchimento sólido com --primary +
  border-color + texto invertido (--on-primary), em vez de depender de
  sombra. Testado no app real (claro e escuro) com 1, 2 e os 3 pills
  ativos ao mesmo tempo — em ambos os temas dá pra ver de relance quais
  estão marcados (ativo = pill sólido de cor forte; inativo = chip claro/
  neutro, contraste alto nos dois sentidos conforme o tema).

- [RESOLVIDO em 08/09/2026] Hierarquia visual do card "notificar por" com
  os 2 campos de contato ativos: e-mail e WhatsApp agora dividem uma
  única caixa "pressed" (#notifContatoGroup, com uma linha divisória
  entre os dois campos só quando os dois estão visíveis) em vez de uma
  caixa "pressed" inteira pra cada um — antes, com os 3 canais ativos, o
  card empilhava rótulo + 3 pills + 2 caixas com sombra/borda própria
  cada uma + lista de status + botão, e lia como vários cartões dentro
  de um cartão. Os rótulos de cada campo também foram encurtados ("e-mail
  pra receber os avisos" → "e-mail", "WhatsApp pra receber os avisos" →
  "WhatsApp"), já que o nome do canal já aparece na pill logo acima e o
  rodapé do card já explica quando os avisos são mandados — repetir a
  frase inteira em cada campo só engordava o bloco sem dar informação
  nova. Testado com push sozinho, cada canal de contato sozinho e os 3
  juntos, em claro e escuro (o card em si é sempre num tom marrom fixo
  --card-bg, independente do tema do app — isso é esperado, não bug).
  Ver index.html: #notifContatoGroup / #notifContatoDivider; app.js:
  renderNotifCanal().

- [RESOLVIDO em 08/09/2026] Tratamento visual da lista de status por
  canal: cada linha de #notifStatusList agora tem uma bolinha (.status-dot,
  7px) antes do texto — verde (--good, variante clara #DCE4B8 sobre o
  card marrom) quando "ativado", terracota (--warn, variante clara
  #F6C79B) nos demais casos (bloqueado, não suportado, precisa cadastrar
  endereço/número, aguardando autorização, nenhum canal selecionado).
  Reaproveita os mesmos tokens já usados em .badge.good/.badge.warn (não
  criou paleta nova). A frase continua explicando por extenso, então a
  cor é reforço visual, não a única pista — acessível pra quem não
  distingue cores. Testado com harness estático usando o style.css real
  (Chrome headless, screenshots) cobrindo as 6 combinações de estado
  (nenhum canal, push em cada status, e-mail/whatsapp faltando, combo
  dos 3), em claro e escuro — bolinha verde e terracota bem distintas
  nos dois temas sobre o --card-bg fixo do card. Ver style.css:
  #notifStatusList / .status-dot; app.js: renderNotifCanal().

- [RESOLVIDO em 08/09/2026] Texto de rodapé do card quebrando feio em
  telas menores: o texto original ("...use o botão acima só pra testar
  o envio.") virava uma linha órfã de uma palavra só ("envio.") sozinha
  embaixo em ~360px de largura (medida bem comum de celular) e esticava
  pra 5 linhas em 320px. Novo texto: "avisos são enviados automaticamente
  quando um insumo ou vela pronta fica abaixo do mínimo — o botão acima
  é só pra testar." — mais curto (tirou a repetição "use o botão... pra
  testar o envio", já que o botão logo acima já diz "testar aviso
  agora"), sem órfã em nenhuma largura testada (320/360/375/390/430px).
  Testado com harness estático usando o style.css real (Chrome headless,
  simulando cada largura via wrapper de largura fixa em vez de depender
  da viewport do navegador, que neste ambiente trava num piso de
  ~500px). Ver index.html: rodapé do card "notificar por", logo após
  o botão "testar aviso agora".

- [RESOLVIDO em 08/09/2026] Conjunto todo da tela "avisos de reposição"
  revisado com o conceito novo (aviso automático, sem depender de
  clique). Achado: o card "notificar por" (config de canais) ocupava o
  topo inteiro (~340px), empurrando a lista de itens abaixo do mínimo —
  o motivo real de alguém abrir essa tela — pra depois da dobra; ainda
  lia como "primeiro configure, depois veja o que precisa de atenção",
  o oposto do conceito novo. Opções foram levantadas com a usuária
  (reordenar / colapsar o card / manter como está) — decidiu por
  reordenar: contador + lista agora vêm logo abaixo do título, e o card
  "notificar por" desceu pra depois da lista, como configuração
  secundária. De quebra, no estado vazio ("tudo certo por aqui") o
  contador "0 itens abaixo do mínimo" foi escondido — repetia a mesma
  informação do empty state duas vezes seguidas logo no topo da tela,
  o que ficou mais evidente com a lista subindo pra primeira posição.
  Testado com harness estático (style.css real) nos 4 cenários (com
  itens / vazio × claro / escuro). Ver index.html: ordem dos blocos
  dentro de data-screen="reposicao"; app.js: renderReposicaoPanel()
  (#reposicaoCount agora com hidden quando zero itens).

===========================================
FUNCIONALIDADES (depois do design)
===========================================

- [RESOLVIDO em 08/09/2026] E-mail real (Resend): domínio
  pontoparagrafo.com.br verificado na Resend (registros DNS de
  SPF/DKIM/DMARC adicionados no provedor do domínio), API key gerada e
  os dois secrets configurados no Supabase (projeto ponto-paragrafo →
  Edge Functions → Secrets): RESEND_API_KEY e RESEND_FROM. Pegadinha
  que travou a primeira tentativa: o secret da key tinha sido salvo como
  "resend_api_key" (minúsculo) — Deno.env.get é case-sensitive, então a
  Edge Function não achava a variável e todo envio de e-mail voltava
  "pendente". Corrigido apagando e recriando o secret com o nome exato
  RESEND_API_KEY. Testado ponta a ponta: chamada direta à Edge Function
  "enviar-aviso" com canais:["email"] retornou {"ok":true} e o e-mail
  chegou de verdade na caixa de teste. Falta só o canal WhatsApp
  (Twilio, ver item abaixo) pros dois canais reais funcionarem.

- [DECIDIDO em 08/09/2026: NÃO FAZER] WhatsApp real (Twilio): usuária
  decidiu não configurar depois de entender os custos — Twilio "Pay as
  you go" exige cartão de crédito cadastrado, cobra aluguel do número
  (~US$1-2/mês) + por conversa iniciada (poucos centavos cada), e o
  "free trial" (sem cartão) não serve pra ativar um WhatsApp Business
  Sender de verdade com template aprovado pela Meta, só pra testar em
  sandbox. Decisão: manter só e-mail (Resend, já funcionando) e push
  como canais reais; WhatsApp fica indisponível no app (a função
  "enviar-aviso" já trata isso sozinha — sem os secrets TWILIO_*, esse
  canal responde "pendente"/"configuração pendente", não quebra nada).
  Se decidir reconsiderar no futuro, os passos originais eram:
  1. Criar conta Twilio (plano "Pay as you go", com cartão)
  2. Ativar um WhatsApp Business Sender
  3. Submeter um template de mensagem pra aprovação da Meta — obrigatório
     porque o aviso é uma mensagem iniciada pela empresa (leva de 1 a
     alguns dias)
  4. No Supabase → Edge Functions → Secrets, adicionar:
       TWILIO_ACCOUNT_SID
       TWILIO_AUTH_TOKEN
       TWILIO_WHATSAPP_FROM   (ex.: whatsapp:+14155238886)
       TWILIO_TEMPLATE_SID    (Content SID do template aprovado)

- [RESOLVIDO em 08/09/2026] Travar quem pode chamar a função de envio: a
  Edge Function "enviar-aviso" (Supabase, projeto ponto-paragrafo) já veio
  com a checagem pronta (achado ao buscar o código: `if (APP_SHARED_SECRET
  && req.headers.get("x-app-secret") !== APP_SHARED_SECRET) return 401` —
  se o secret não existir, a função ignora a checagem e aceita qualquer
  chamada). O que faltava e foi feito: app.js agora manda o header
  x-app-secret em toda chamada (ver NOTIF_APP_SECRET, logo abaixo de
  NOTIF_ANON_KEY, e o header em enviarAvisoServidor). Testado com curl
  direto na função com um header errado → respondeu 200 normal,
  confirmando que o secret AINDA NÃO estava cadastrado no Supabase (a
  trava existia no código mas estava inativa — nada quebrou com essa
  mudança). Passo manual que faltava (só a usuária conseguia fazer, é um
  secret): cadastrado no Supabase → projeto ponto-paragrafo → Edge
  Functions → enviar-aviso → Secrets:
    APP_SHARED_SECRET = 107874553e13cd887402d09fe415a5641cff9d20fd78bba0
  (idêntico ao já hardcoded em NOTIF_APP_SECRET no app.js). Usuária
  confirmou: testou com "testar aviso agora" depois de cadastrar e
  continuou enviando normal, sem erros — a trava agora está ativa de
  verdade.

===========================================
ESTRUTURA GERAL DO APP — pendências importantes
(adicionado em 04/09/2026, varredura do app inteiro, não só o painel de avisos)
===========================================

-- Design --

- [JÁ ESTAVA RESOLVIDO — só não tinha sido marcado aqui, achado em
  08/09/2026 ao checar esta pendência] Tela "mais": esta pendência
  descrevia um estado antigo (só alternância de tema + sair da conta) —
  o app real já não bate com essa descrição há um tempo. Conferido
  index.html/app.js hoje: a tela "mais" tem 4 cartões de verdade —
  "configurações" (data-goto="configuracoes", receita padrão/jarra/
  preços — já existia antes desta pendência), "conta" (nome/e-mail +
  troca de senha com os 3 campos e "mostrar senha", renderContaForm/
  submitContaDadosForm/submitContaSenhaForm), "backup dos dados"
  (exportar/restaurar .json, já resolvido em 04/09) e "ajuda" (5
  perguntas frequentes em <details>/<summary>, .faq-item no style.css,
  cobrindo aviso de reposição, perda de derretimento, onde ficam os
  dados, backup e "esqueci minha senha") — mais o botão "sair da
  conta". Nada ficou faltando do que esta pendência pedia; não precisou
  de nenhuma mudança de código, só corrigir o registro aqui.

- [RESOLVIDO em 08/09/2026] Card "relatório completo" do dashboard: deixou
  de ser um toast "em breve" e virou uma tela de verdade (data-goto=
  "relatorioCompleto"), última pasta/atalho de Vendas que faltava
  desenhar. Escopo combinado com a usuária (perguntado com 4 opções:
  detalhamento de vendas / ranking de clientes / custos e margem
  detalhados / comparação com período anterior) — decidiu só
  "detalhamento de vendas". Usa o MESMO período do Dashboard
  (periodoState/isInPeriodo), sem seletor de período próprio — mesmo
  padrão já usado em Controle de Custos ("ajuste no Dashboard"). Três
  blocos, todos lendo vendasState.vendas na hora (nada duplicado):
  "vendido por variação" (quantidade × preço de tabela, antes de cupons —
  mesma base do card "total de vendas"), "por forma de pagamento" (Pix/
  Cartão/Dinheiro, já com cupom descontado — mesma base de "receita
  real") e "cupons usados" (código, nº de usos, desconto total dado por
  cupom). Os 2 primeiros blocos têm uma barra de proporção por linha
  (reaproveita só --line/--primary, sem CSS novo). Ver app.js:
  "Relatório completo — módulo 19"; index.html: data-screen=
  "relatorioCompleto". CONFIRMADO no app real em 09/09/2026 (login de
  verdade via Supabase Auth com a conta da usuária, Playwright/Chromium
  headless servindo os arquivos estáticos com "python3 -m http.server"
  — node/npx continuam ausentes nesta sessão, mas dava pra rodar
  headless mesmo assim): os 3 blocos batem com o resto do app no mesmo
  período de 7 dias — "vendido por variação" somou R$ 1.185,00 = "total
  de vendas" do Dashboard; "por forma de pagamento" somou R$ 1.161,30 =
  "receita real" do Dashboard = "vendido" do Histórico de Vendas;
  "cupons usados" mostrou PAUSA10 com -R$ 23,70, o mesmo desconto da
  venda com cupom no Histórico. Testado também um período sem venda
  nenhuma (Dashboard → personalizado → jan/2020): os 3 blocos mostraram
  o empty state certo ("nenhuma venda no período" / "nenhum cupom
  usado"). Zero erros de console em toda a navegação.

- [RESOLVIDO em 08/09/2026] Cupons: pasta "Vendas → Cupons" deixou de
  ser um toast "em breve" e virou 2 telas de verdade (lista + novo/
  editar). Cadastro completo do cupom que "Registrar Venda" já sabia
  aplicar mas não tinha onde nascer (cuponsState existia só como seed
  no código, sem UI nenhuma): código (único, comparado sem diferenciar
  maiúscula/minúscula), tipo percentual ou fixo, início e fim de
  validade opcionais, limite de uso opcional (0 = sem limite) e status
  ativo/inativo. Cada linha da lista mostra um badge calculado (ativo/
  inativo/vencido/esgotado — não só o campo bruto "ativo", também olha
  data de validade e limite×usos) e a contagem de usos; tocar num
  cupom abre editar direto, sem tela de detalhe separada (não fazia
  sentido duplicar pra um cadastro tão simples). Reaproveita os mesmos
  padrões visuais de Encomendas/Registrar Venda (raised-sm, pressed
  field, pills, badge good/warn) — nenhum CSS novo. Ver app.js:
  "Cupons — módulo 17"; index.html: data-screen="cupons" /
  "cupomForm". Testado ponta a ponta com Playwright/Chromium headless:
  criar cupom fixo (R$ 15,00, limite 5 usos), bloquear cadastro de
  código duplicado (PAUSA10 já existente, mesmo digitado em minúscula)
  sem sair do formulário, editar o cupom novo pra inativo (badge muda
  na lista) e excluir — zero erros de console em toda a sequência.

- [RESOLVIDO em 08/09/2026] Controle de Custos: pasta "Vendas →
  Controle de Custos" deixou de ser um toast "em breve" e virou uma
  tela de verdade. Card "custo real por vela" (custo de insumo médio
  dos lotes + custo fixo rateado) com o par de indicadores detalhando
  cada metade; "lucro líquido real" no mesmo período do Dashboard
  (periodoState compartilhado — a tela não duplica o seletor de
  período, só mostra o valor e diz "ajuste no Dashboard"); "margem por
  variação" (preço de venda cadastrado − custo real por vela, com aviso
  em --warn quando dá prejuízo); "custos fixos mensais" — primeira tela
  que expõe custosState.fixos e rateioBase pra edição (até então só
  existiam como dado de seed no código, sem UI nenhuma) — lista
  editável (nome + valor, adicionar/remover linha) + produção mensal
  estimada pro rateio, salvos em lote com um botão só (mesmo padrão de
  Configurações de produção: inputs soltos enquanto digita, sem
  recálculo ao vivo). Reaproveita os mesmos padrões visuais do
  Dashboard/Encomendas (raised card destacado, par de raised-sm) —
  nenhum CSS novo. Ver app.js: "Controle de Custos — tela — módulo 18";
  index.html: data-screen="controleCustos". CONFIRMADO no app real em
  09/09/2026 (mesmo setup: login de verdade, Playwright/Chromium
  headless servindo os arquivos estáticos): "lucro líquido real" mostrou
  R$ 144,30, batendo com o card do Dashboard no mesmo período de 7 dias;
  "custo real por vela" R$ 87,10 (insumo R$ 7,60 + fixo R$ 79,50
  rateado por 40 velas/mês). Achado de negócio real da usuária (não é
  bug do app): as 4 variações apareceram todas em --warn/prejuízo (ex.:
  "Pera e Fresia · âmbar" vende a R$ 79,00 mas custa R$ 87,10 real,
  -10%) — vale ela revisar preço ou o rateio de custo fixo. NÃO testado
  nesta rodada adicionar/remover/salvar um custo fixo novo, pra não
  alterar o rateio real da produção só pra teste — combinar com a
  usuária antes de fazer esse teste com dados de verdade. Zero erros de
  console.

- [RESOLVIDO em 08/09/2026] Histórico de Vendas: pasta "Vendas →
  Histórico de Vendas" deixou de ser um toast "em breve" e virou uma
  tela de verdade — lista somente-leitura de toda venda já registrada
  (tanto pelas de Registrar Venda quanto pelas Encomendas entregues),
  sem tela de detalhe própria (não fazia parte do pedido). Busca por
  cliente ou variação (um só campo, comparando os dois) + pills de
  período (7 dias / 30 dias / tudo, default "tudo" — local a esta tela,
  não mexe no seletor de período do Dashboard) + par de indicadores
  "vendido" / "confirmadas" que recalcula pro filtro ativo. Cada linha
  mostra cliente, itens, data, forma de pagamento, cupom aplicado (se
  teve) e "via encomenda" (se veio de uma entrega), com badge
  confirmada/cancelada. Reaproveita os mesmos padrões visuais de
  Encomendas/Clientes (raised-sm, pressed.field.row de busca, pills,
  badge good/warn) — nenhum CSS novo. Ver app.js: "Histórico de Vendas
  — módulo 16"; index.html: data-screen="historicoVendas". CONFIRMADO
  no app real em 09/09/2026 (login de verdade, Playwright/Chromium
  headless): pill "7 dias" mostrou 5 vendas confirmadas somando
  R$ 1.161,30 — bate com "receita real" do Dashboard no mesmo período;
  busca por "a" filtrou a lista ao vivo sem recarregar a tela; badges
  confirmada/cancelada e o "cupom PAUSA10" aparecendo certo na linha que
  usou. Zero erros de console.

- [RESOLVIDO em 08/09/2026] Ícones do card "resumo de hoje" no dashboard
  sem contraste: usuária gostou dos ícones (produção/insumos/vendas) mas
  reportou que a cor não contrastava. Causa: o círculo de fundo do ícone
  (.resumo-row-icon, classe .raised-sm) herdava background:var(--card-bg)
  — no tema claro é o MESMO marrom do próprio card (#84592B), círculo
  invisível; no escuro vira #5A3D27, quase idêntico ao --primary
  (#442D1C) que a paleta fixa do dashboard força pro traço do ícone —
  medido 1.3:1 de contraste no escuro e 2.1:1 no claro, os dois abaixo do
  mínimo de 3:1 do WCAG 2.1 AA pra elementos gráficos. Troca: mesmo par
  já usado no hero pra ícone-sobre-cor-sólida (.dash-hero .avatar/
  .iconbtn) — chip translúcido creme (rgba(243,230,206,.14)) + traço
  creme sólido (#F3E6CE) em vez de --card-bg/--primary — resultando em
  3.8:1 (claro) e 8:1 (escuro) contra o chip, círculo visível nos dois
  temas. Contraste calculado manualmente (luminância relativa WCAG) nos
  dois pares antes e depois da troca; sem harness de screenshot disponível
  nesta sessão (node/npx ausentes) — vale confirmar visualmente no app
  real na próxima vez que abrir. Ver style.css: .dash-resumo
  .resumo-row-icon.

-- Funcionalidades --

- [RESOLVIDO em 04/09/2026; regrediu e foi reconstruído em 08/09/2026]
  Cadastro de venda de verdade: tela "Registrar Venda" (Vendas →
  Registrar Venda) implementada — cliente (ou avulso), itens por
  variação com estoque ao vivo, forma de pagamento, cupom opcional,
  resumo com desconto/total. Ao confirmar, debita producaoEstoqueState
  na hora e grava em vendasState de verdade; dashboard, ranking de
  clientes e histórico já leem isso sozinhos. Ver app.js: módulo
  "Registrar Venda — módulo 14"; index.html: data-screen="registrarVenda".
  Em 08/09/2026 essa tela foi perdida numa sobrescrita acidental de
  index.html (virou só a palavra "placeholder") e o restore por git
  trouxe um commit anterior à existência dela — reconstruída no mesmo
  dia reaproveitando os IDs/data-actions que o app.js já esperava
  (nenhuma lógica JS tinha sido perdida, só o HTML). Testado ponta a
  ponta com Playwright/Chromium headless (criar conta → Vendas →
  Registrar Venda → cliente avulso → item → forma de pagamento →
  confirmar), toast "venda de R$ 158,00 registrada.", zero erros de
  console. Commitado (39b0720).

- [RESOLVIDO em 08/09/2026] Encomendas: pasta "Vendas → Encomendas"
  deixou de ser um toast "em breve" e virou 3 telas de verdade (lista,
  novo/editar, detalhe). Fluxo pendente → em produção → entregue: criar
  uma encomenda (cliente ou avulso, itens por variação, entrega
  prevista opcional, observações opcionais) NÃO mexe em estoque — é só
  uma promessa de venda futura; avançar pra "em produção" só muda o
  status (com um lote existente da Produção vinculável, opcional);
  avançar pra "entregue" é que gera a venda de verdade — revalida o
  estoque de velas prontas na hora (bloqueia com toast se não tiver o
  suficiente, sem deixar converter estoque negativo), debita
  producaoEstoqueState e grava em vendasState.vendas com
  origemEncomendaId apontando de volta pra a encomenda (campo que já
  existia, sem uso, desde o módulo de vendas) — dashboard, ranking de
  clientes e histórico já contam a entrega sozinhos, sem tela extra.
  Lista com filtro por status (todas/pendente/em produção/entregue),
  cada linha mostra cliente, itens, entrega prevista e total; forma de
  pagamento só é perguntada na hora de marcar como entregue (pill
  Pix/Cartão/Dinheiro), já que na criação da encomenda ainda não houve
  pagamento. Reaproveita os mesmos padrões visuais de Registrar
  Venda/Clientes (raised-sm, badge good/warn/primary, pills, picker de
  cliente) — nenhum CSS novo. Ver app.js: "Encomendas — módulo 15";
  index.html: data-screen="encomendas" / "encomendaForm" /
  "encomendaDetalhe". Testado ponta a ponta com Playwright/Chromium
  headless em claro e escuro (a troca de tema do app ignora
  prefers-color-scheme do navegador — tem toggle próprio em pp_tema_v1,
  default "claro" — o teste força o toggle da tela "mais" pra cobrir o
  escuro de verdade): lista com as 3 encomendas de exemplo, filtro por
  status, criar uma encomenda nova (contador 3→4), tentar entregar uma
  encomenda com item sem estoque suficiente (bloqueado com toast, status
  não muda) e o ciclo completo pendente→em produção→entregue de outra
  (vira venda, nota "já virou uma venda confirmada" aparece). Zero erros
  de console nas duas rodadas.

- [RESOLVIDO em 04/09/2026] Backup / exportação dos dados: cartão
  "backup dos dados" na tela "mais" — "exportar backup (.json)" baixa
  um arquivo com insumos, catálogo, produção, lotes, cupons, vendas,
  custos e clientes (conta/senha/sessão ficam de fora de propósito);
  "restaurar backup" lê o arquivo, confirma antes de sobrescrever
  (ação destrutiva) e recarrega o app com os dados restaurados.
  Testado ponta a ponta, sem bugs. Ver app.js: "Backup / exportação
  dos dados" (BACKUP_KEYS, exportarBackup, importarBackupFile).

- [RESOLVIDO em 09/09/2026 — implementado em 08/09, testado ponta a
  ponta com sucesso em 09/09, ver detalhes no fim] Sincronização entre
  dispositivos. Decisões
  tomadas com a usuária (3 perguntas): (1) login real via Supabase Auth
  (e-mail+senha), substituindo a conta local fake de antes — mesmo
  login em qualquer aparelho; (2) sincronização em "blob único" (sobe/
  baixa tudo de uma vez, mesmo formato do backup .json), sem sync por
  registro nem em tempo real; (3) aceitável exigir internet só pra
  entrar/criar conta — o resto do app continua funcionando offline
  depois de logado.

  O QUE MUDOU:
  - app.js módulo 9 ("Conta / sessão") inteiro reescrito: submitSignup/
    submitLogin agora chamam a API REST do Supabase Auth direto (fetch,
    sem SDK — mesmo padrão que a Edge Function de avisos já usava:
    ver "Supabase — config compartilhada", SUPABASE_URL/
    SUPABASE_ANON_KEY, reaproveitado pelo módulo de notificação
    também, que antes tinha sua própria cópia da anon key).
  - sessaoState agora guarda accessToken/refreshToken/expiresAt/userId
    de verdade (Supabase), não um "logado:true" fake. checkAuthAndInit
    tenta renovar o token sozinho (supaRefreshSessao) se tiver vencido
    ao abrir o app; se a renovação falhar, volta pro login.
  - contaState não guarda mais senha nenhuma (nem em texto puro, nem de
    outro jeito) — resolve de quebra a pendência antiga da senha em
    texto puro no localStorage, que não precisa mais existir como item
    separado nesta lista.
  - "trocar senha" (tela Conta) reautentica com a senha atual antes de
    trocar (Supabase Auth não tem endpoint próprio de "confirma senha
    atual" — reautenticar é o jeito recomendado pela documentação
    deles). "esqueci a senha" (tela de login) agora dispara um e-mail
    de recuperação de verdade via /auth/v1/recover — MAS a tela de
    "definir nova senha" que o link do e-mail deveria abrir ainda NÃO
    foi construída (ver "NÃO TESTADO" abaixo).
  - E-mail da conta virou somente-leitura na tela Conta (mudar e-mail
    exigiria um fluxo de confirmação por e-mail, fora do escopo desta
    sessão — só nome é editável por lá agora).
  - Sincronização de verdade (módulo 20, novo): buildEstadoDados/
    aplicarEstadoDados (extraídos do módulo de Backup, que passou a
    reusar as duas — mesma lista BACKUP_KEYS pros dois, sem duas listas
    divergentes) montam/aplicam o mesmo pacote de dados de negócio
    (insumos, catálogo, produção, lotes, cupons, vendas, custos,
    clientes — conta/senha continuam de fora) que o backup .json já
    usava. supaEstadoPush sobe esse pacote pra tabela "estado" do
    Supabase (1 linha por usuário, RLS já configurada — ver políticas
    "usuario ve/insere/atualiza seu proprio estado", conferidas nesta
    sessão, já corretas, nada mudou nelas). Disparo: PUSH ao criar
    conta (primeiro aparelho — não tem nada pra puxar ainda), PUSH ao
    sair da conta (logout), PUSH manual pelo botão "sincronizar agora"
    (tela Conta, mostra a hora da última sincronização deste
    aparelho). PULL só acontece no login, com window.confirm antes de
    sobrescrever os dados deste aparelho (se cancelar, o login segue
    normal mantendo os dados daqui).
  - Tela "mais" → "ajuda" (FAQ) e a tela Conta tiveram os textos
    atualizados pra não falar mais em "conta só local, sem
    sincronização" (estava certo antes, ficou errado depois desta
    mudança).

  LIMITAÇÃO CONHECIDA (aceita pela usuária, registrar pra não
  redescobrir depois): sem merge de verdade entre aparelhos — é
  "último push vence". Se os dois aparelhos editarem coisas diferentes
  offline antes de sincronizar, quem sincronizar por último sobrescreve
  o que o outro tinha mandado. Pro uso combinado (sincronizar ao
  terminar uma sessão de trabalho, não no meio dela) não deve ser
  problema na prática.

  TESTADO nesta sessão, direto contra a API real do Supabase via curl
  (sem node/npx/navegador — nenhum dos dois disponível, então não deu
  pra clicar de verdade na interface, mas o contrato com o backend foi
  validado de ponta a ponta):
  - Confirmado: o projeto EXIGE confirmação por e-mail pra criar conta
    (GET /auth/v1/settings → "mailer_autoconfirm": false). O caminho do
    código que trata isso ("conta criada! confira seu e-mail...") é o
    que roda de verdade em todo signup, não é só um caso teórico.
  - Confirmado: POST /auth/v1/signup com um e-mail válido responde 200,
    grava o nome em user_metadata certinho, sem access_token (bate com
    o que o código espera).
  - Confirmado: RLS da tabela "estado" bloqueia acesso sem login (pedido
    anônimo devolve lista vazia, não vaza dado de ninguém).
  - Confirmado: erro de login errado vem como esperado ("Invalid
    credentials incorrectos" tratado certo).
  - Corrigido um bug real encontrado no teste: a mensagem de rate limit
    que a Supabase manda de verdade ("For security purposes, you can
    only request this after 40 seconds.") não batia com o regex antigo
    do código — ia aparecer em inglês pro usuário. supaErroMsg agora
    prioriza o campo error_code da resposta (mais estável) em vez de só
    casar texto solto.

  [RESOLVIDO em 09/09/2026] Envio de e-mail da Auth estava no limite
  padrão da Supabase (2/hora — serviço compartilhado/gratuito, diferente
  do Resend que já era usado pros avisos de reposição). Usuária
  configurou SMTP customizado em Authentication → Emails → SMTP
  Settings, reaproveitando a MESMA conta Resend (host smtp.resend.com,
  porta 587, usuário "resend", senha = uma API key da Resend). Travou
  numa primeira tentativa: o campo "Sender email address" tinha um
  typo (`pontoparagrafo@com.br`, faltando o "pontoparagrafo." antes do
  ".com.br") — a Resend recusava com "The com.br domain is not
  verified" porque via só "com.br" como domínio, que nunca foi
  verificado. Corrigido pra `contato@pontoparagrafo.com.br`. Depois do
  fix, o log da Supabase confirmou o limite subindo de 2/hora pra
  30/hora — SMTP customizado ativo de verdade.

  [TESTADO em 09/09/2026, PONTA A PONTA, COM SUCESSO] Fluxo completo de
  login + sincronização, feito 100% via API real (curl + Gmail
  conectado, sem navegador — não havia node/npx/chromium nesta sessão,
  mas todo o contrato com o backend que o app.js usa foi validado de
  verdade, chamada por chamada):
  1. POST /auth/v1/signup com clarasallesgv+testesync2@gmail.com → 200,
     e-mail de confirmação chegou de verdade na caixa (via Resend/SMTP
     customizado, remetente contato@pontoparagrafo.com.br).
  2. E-mail lido direto pelo Gmail conectado — GET do link de
     confirmação (extraído do RAW da mensagem, decodificando
     quoted-printable manualmente, já que o corpo HTML/texto vinha com
     "=3D" no lugar de "=") → devolveu access_token + refresh_token de
     verdade (redirect com sessão no fragmento da URL, comportamento
     padrão do Supabase Auth).
  3. GET /auth/v1/user com o access_token → sessão confirmada válida
     (email_confirmed_at preenchido).
  4. POST /rest/v1/estado?on_conflict=user_id (mesmo formato exato que
     supaEstadoPush manda) com um "cliente de teste" → 201 Created.
  5. GET /rest/v1/estado?user_id=eq.<uid>&select=dados → devolveu
     exatamente o dado subido no passo 4 — RLS permitindo o dono ler a
     própria linha, PUSH e PULL confirmados funcionando de ponta a
     ponta.
  6. POST /auth/v1/token?grant_type=password com o mesmo e-mail/senha
     (já confirmado) → 200, login normal funcionando (mesma chamada que
     submitLogin faz).
  Não testado ainda por essa via (exigiria abrir o app de verdade num
  navegador, não só a API): a UI em si (telas, toasts, o
  window.confirm de "encontrei dados na nuvem...") e trocar senha pela
  tela Conta. A tela de "definir nova senha" que o link de "esqueci a
  senha" abre já foi construída — ver pendência própria logo abaixo
  desta ("Tela de 'definir nova senha'"). IMPORTANTE pra quem já
  usava o app antes desta mudança: a conta local antiga não é
  reconhecida mais — abrir o app cai direto no login/criar conta de
  novo (esperado, dados de negócio no localStorage continuam
  intactos).

  LIMPEZA PENDENTE (opcional, não afeta nada): sobraram 3 contas de
  teste no Supabase Auth deste projeto — 1 não confirmada
  (teste-claude-sync-<timestamp>@gmail.com, e-mail inventado) e 2
  confirmadas (clarasallesgv+testesync@gmail.com e +testesync2, aliases
  do seu Gmail real — a segunda tem 1 linha de teste na tabela
  "estado" com um cliente fake "Cliente Teste Sync"). Nenhuma interfere
  com a conta de verdade que a usuária for criar pra loja. Pode apagar
  em Supabase Dashboard → Authentication → Users quando quiser, ou
  deixar.

  Ver app.js: "Supabase — config compartilhada", "Conta / sessão —
  módulo 9", "Sincronização entre aparelhos — módulo 20"; index.html:
  data-screen="conta" (bloco "sincronização entre aparelhos").

- [RESOLVIDO em 09/09/2026] Tela de "definir nova senha" (a que faltava
  construir depois do "esqueci a senha" — ver pendência de sincronização
  acima). Construída como um terceiro auth-screen em #authView
  (data-authscreen="novaSenha", ao lado de "login" e "criarConta"), com
  os mesmos campos/estilo de "trocar senha" da tela Conta (nova senha +
  confirmar, com "mostrar senha").

  Como abre sozinha: o link do e-mail de recuperação do Supabase Auth
  redireciona de volta pro app com #access_token=...&type=recovery no
  fragmento da URL (fluxo padrão do GoTrue, sem SDK). detectRecoveryLink()
  (chamada no início de checkAuthAndInit, antes de checar sessão salva)
  reconhece isso, guarda o access_token numa variável só desta carga de
  página (não vai pro localStorage — a senha ainda não foi confirmada) e
  já limpa o fragmento da URL, e abre a tela "novaSenha" direto — mesmo
  que já exista uma sessão válida salva neste aparelho, o link tem
  prioridade. submitNovaSenha() troca a senha com esse token via PUT
  /auth/v1/user e, dando certo, já loga a pessoa (aplicarSessaoLogin,
  reaproveitando o próprio objeto "user" que o PUT devolve) e puxa os
  dados da nuvem como um login normal — não precisa digitar a senha nova
  de novo. Link expirado/já usado (401) cai numa mensagem própria
  orientando a pedir um novo link no login.

  DEPENDE DE CONFIGURAÇÃO FORA DO CÓDIGO: o "Site URL"/Redirect URLs em
  Supabase Dashboard → Authentication → URL Configuration precisa
  apontar pra URL onde este app está hospedado, senão o link do e-mail
  redireciona pra outro lugar e nunca chega nesta tela nova — vale
  conferir lá antes de testar de verdade com um e-mail real.

  NÃO TESTADO nesta sessão num navegador de verdade (sem
  node/npx/chromium disponíveis aqui, mesma limitação da sessão anterior
  — ver pendência de sincronização acima): a implementação segue o
  mesmo contrato de resposta do Supabase Auth já validado por essa
  sessão anterior via curl (POST /auth/v1/token, PUT /auth/v1/user), mas
  o fluxo ponta a ponta clicando no link do e-mail de verdade ainda
  precisa ser conferido na prática.

  Ver app.js: "Link de recuperação de senha" (detectRecoveryLink,
  submitNovaSenha, perto de checkAuthAndInit); index.html:
  data-authscreen="novaSenha".

- [RESOLVIDO em 09/09/2026] Integração com a Nuvemshop (site oficial
  da loja): decidido o Cenário 1 — cliente que se cadastra na loja online
  cai sozinho na lista de Clientes do webapp (módulo 13), via webhook
  (customer/created). Cenário 2 (login automático/SSO) fica como projeto
  futuro separado, porque exigiria reformular toda a autenticação do app.
  Isso deixou de esbarrar no bloqueio antigo ("webapp não tem contas nem
  dados em servidor") porque a sincronização entre aparelhos (acima) já
  trouxe Supabase Auth de verdade pro app.

  O QUE JÁ FOI FEITO (lado do código):
  - Projeto Supabase ponto-paragrafo: duas tabelas novas,
    nuvemshop_config (guarda o access_token da loja, só server-side) e
    clientes_nuvemshop (clientes recebidos via webhook, com cursor
    recebido_em) — RLS ligado, sem nenhuma policy (só as Edge Functions,
    via service_role, tocam nessas tabelas; o frontend nunca lê direto).
  - Três Edge Functions novas, publicadas e testadas (smoke test manual —
    ver abaixo o que falta pra teste ponta a ponta real):
      nuvemshop-oauth-callback — troca o "code" do OAuth por access_token
        e já registra sozinha o webhook de customer/created.
      nuvemshop-webhook — recebe o webhook, verifica a assinatura HMAC
        (header x-linkedstore-hmac-sha256) antes de confiar em qualquer
        coisa, busca o cliente completo na API da Nuvemshop e grava em
        clientes_nuvemshop.
      listar-clientes-nuvemshop — o webapp consulta essa função (não a
        tabela direto) pra puxar só os clientes novos desde o último
        cursor salvo em localStorage (pp_clientes_sync_v1). Publicada com
        verify_jwt=true, e ainda confirma por conta própria (chamando
        /auth/v1/user) que quem chamou é uma sessão de usuário de
        verdade — a anon key sozinha não basta, porque é um JWT válido
        pro projeto mesmo sem ninguém logado.
  - app.js módulo 13: syncClientesFromNuvemshop() (perto de
    loadClientesState()/findCliente) mescla os clientes novos em
    clientesState.clientes, dedup por nuvemshopId (undefined pra
    clientes cadastrados manualmente). Disparada ao abrir a tela de
    Clientes (dentro de showScreen) e uma vez no boot (dentro de
    enterApp). Estilo best-effort/silencioso, igual
    enviarAvisoServidor: sem retry, sem timeout, falha de rede não
    aparece pra usuária.

  OS 5 PASSOS MANUAIS FORAM COMPLETADOS em 09/09/2026:
  1. Redirect URI registrada no Nuvemshop Partners: o app tinha 2 campos
     de URL ("Site do aplicativo" e "URL de redirecionamento após
     instalação") — o segundo é o que a Nuvemshop realmente usa pra
     mandar o `code` do OAuth (confirmado na documentação oficial, ver
     https://dev.tiendanube.com/pt/docs/applications/authentication:
     "o link gerado automaticamente no campo URL de redirecionamento
     após a instalação serve apenas pra apoiar nos testes, mas não deve
     ser usado em produção"). Trocado o valor de teste da própria
     Nuvemshop pela URL de verdade da função
     (.../functions/v1/nuvemshop-oauth-callback). Escopo read_customers
     confirmado marcado em Permissões → Customers (write_customers
     ficou desmarcado, como esperado).
  2. NUVEMSHOP_CLIENT_ID e NUVEMSHOP_CLIENT_SECRET cadastrados nos
     secrets do Supabase — confirmado sem expor o valor: chamando
     nuvemshop-oauth-callback sem nenhum parâmetro, ela respondeu
     "faltou o código de autorização" (400) em vez de "integração não
     configurada" (500), que é o erro que ela devolveria se os secrets
     não existissem.
  3. Loja é .com.br (Real/Brasil) — confirmado com a usuária, nenhuma
     mudança de código necessária (API_HOST já estava certo nas duas
     funções que chamam a API da Nuvemshop).
  4. Link de autorização visitado (https://www.tiendanube.com/apps/
     <app_id>/authorize) — OAuth completado com sucesso: loja #7997572
     autorizada, token salvo em nuvemshop_config, webhook de
     customer/created registrado sozinho pela própria função. Conferido
     direto na tabela (sem expor o token): scope
     "write_products,read_customers" (o read_customers necessário está
     lá; o write_products deve ter vindo de outra seção de permissões
     marcada por padrão no app Nuvemshop, não afeta nada aqui porque
     nenhuma função usa escrita de produtos — dá pra desmarcar no
     Partners por princípio de menor privilégio, se quiser).
  5. Cliente de teste ("Clara Gava") cadastrado na loja — chegou em
     clientes_nuvemshop 1 segundo depois via webhook (sem ação manual),
     e apareceu na tela Clientes do webapp ao abrir a tela, confirmado
     pela usuária. Integração ponta a ponta funcionando de verdade.

- [RESOLVIDO em 08/09/2026, de quebra] Segurança da senha da conta:
  antes a senha ficava em texto puro no localStorage (pp_conta_v1),
  legível por qualquer um com acesso ao navegador/devtools do
  aparelho. Resolvida como efeito colateral da "Sincronização entre
  dispositivos" (ver pendência própria) — a senha não é mais guardada
  neste aparelho de jeito nenhum, fica só no Supabase Auth (com hash,
  do lado do servidor); pp_conta_v1 hoje só guarda nome/e-mail (cache
  de exibição, sem dado sensível).

- [RESOLVIDO em 09/09/2026] PWA / instalável: o app já era desenhado
  como um "app" (layout mobile fixo, navegação por dock), mas faltava
  manifest.json + ícones + service worker pra virar de fato instalável
  na tela inicial do celular e funcionar offline. Implementado:
  - assets/icons/icon.svg (fonte, cantos arredondados) e
    icon-maskable.svg (fonte, quadrado até a borda — quem arredonda é o
    próprio SO) reproduzem a marca ".parágrafo" (bolinha + traço, mesmas
    proporções de .wg-mark-dots em style.css) em creme sobre o marrom
    de fundo do app. Rasterizados em PNG (192/512 "any", 192/512
    "maskable", apple-touch-icon 180×180, favicon 32×32) com `qlmanage
    -t` (gerador de thumbnail do QuickLook do macOS, usado como
    conversor SVG→PNG já que node/npx/rsvg-convert/imagemagick/
    cairosvg não estavam disponíveis nesta sessão) — todos conferidos
    visualmente, inclusive o favicon de 32px (a marca continua legível
    em miniatura).
  - manifest.json (raiz do projeto): nome, ícones, theme_color/
    background_color #442D1C (mesmo tom do --bg), display "standalone",
    start_url/scope na raiz.
  - sw.js (raiz): service worker com cache do "app shell"
    (index.html/style.css/app.js/manifest/ícones/linen-brown.jpeg).
    Estratégia: navegação (a página) network-first com fallback pro
    cache; estático do próprio app cache-first; Google Fonts (outra
    origem) stale-while-revalidate; qualquer outra origem (Supabase) ou
    método não-GET passa direto pra rede, sem cache. CACHE_VERSION
    controla a limpeza do cache antigo no "activate".
  - index.html: <link rel="manifest">, favicon (PNG + SVG), apple-
    touch-icon e metas apple-mobile-web-app-*/theme-color no <head>.
  - app.js: registro do service worker em "PWA / instalável — módulo
    21" (roda sempre, logado ou não; feature-detect, silencioso se
    falhar).
  Testado ponta a ponta com Playwright/Chromium headless contra um
  servidor local (python3 -m http.server, já que node/npx seguiam
  ausentes): manifest.json válido (JSON parseado, content-type
  application/json), sw.js servido com content-type text/javascript
  (exigido pelo navegador pra aceitar o register()), service worker
  registra e ativa (reg.active.state === "activated", scope correto),
  link de manifest/apple-touch-icon/theme-color presentes no DOM, zero
  erros de console na carga normal. Testado offline (context.set_offline
  no Playwright): já na 1ª visita o app shell (HTML/CSS/JS/textura de
  fundo) carrega 100% do cache do service worker offline — só as fontes
  do Google (outra origem) ainda falham offline logo na 1ª visita,
  porque a 1ª carga de uma página nunca é controlada pelo seu próprio
  service worker (comportamento padrão de todo browser, não bug deste
  código: o SW só passa a interceptar as requisições a partir da 2ª
  visita em diante). Confirmado o comportamento esperado: repetindo o
  teste com 2 cargas online antes de ir offline, a 3ª carga (offline)
  veio 100% do cache, zero requisições falhando, inclusive as fontes.
  NÃO testado: instalação de verdade (ícone na tela inicial) num
  celular/desktop real — precisa do app publicado atrás de HTTPS (ou
  rodando em localhost); abrir index.html direto como arquivo (file://)
  não registra o service worker (o navegador não permite), o que é
  esperado e não é bug. Ver assets/icons/, manifest.json, sw.js;
  index.html: <head>; app.js: "PWA / instalável — módulo 21".

- [RESOLVIDO em 08/09/2026] Adicionar calendário no dashboard: tira dos 7 dias no hero
  (#dashSemanaStrip) agora é tocável e abre a tela "calendário" (novo data-screen), com
  mês completo (grade dom-sáb, setas pra mês anterior/próximo), dia de hoje contornado,
  dia selecionado preenchido sólido (mesmo princípio do .pill.active — cor sólida = estado
  ativo) e uma bolinha embaixo dos dias que têm venda confirmada. Ao tocar em qualquer dia,
  aparece embaixo o "recap do dia" (produção/insumos/vendas), mesma conta do card "resumo
  de hoje" do dashboard, só que pra qualquer data — a linha de vendas ali soma quantas
  velas foram vendidas naquele dia, não só quantas vendas. Abre sempre no mês corrente com
  hoje pré-selecionado, então já mostra um recap antes de qualquer toque. Testado com
  harness estático usando o style.css real (Chrome headless, screenshots, setembro 2026)
  em claro e escuro — grade, dia de hoje, dia selecionado e bolinhas de venda todos
  legíveis nos dois temas (modo claro foi removido do app depois, ver pendência "Modo claro
  removido" logo abaixo). CONFIRMADO dentro do app real em 09/09/2026 (login de verdade via
  Supabase Auth com a conta da usuária, Playwright/Chromium headless servindo os arquivos
  estáticos com "python3 -m http.server"): tocar em #dashSemanaStrip abre a tela, a grade de
  setembro/2026 carrega certo, dia 9 (hoje) contornado, um dia com bolinha (dia 3) é clicável
  e vira o selecionado sólido, e o recap do dia daquele dia bate com a mesma conta do card
  "resumo de hoje" do dashboard (produção/insumos/vendas). Zero erros de console. Ver index.html: data-screen="calendario",
  #dashSemanaStrip; app.js: abrirCalendario/renderCalendarioGrid/renderCalendarioRecap/
  computeResumoDia; style.css: .cal-monthname/.cal-grid/.cal-day.

- [RESOLVIDO em 08/09/2026] Modo claro removido do app inteiro — decisão
  da usuária, não uma correção de bug: o app vai ser só escuro daqui pra
  frente, não só "escuro por padrão" com um claro ainda alternável.
  Removidos: o switch visual na tela "mais" (index.html), o script
  inline no <head> que aplicava o tema salvo antes do 1º paint, e toda a
  lógica em app.js (TEMA_KEY/loadTema/applyTema/temaState/setTema + o
  listener de [data-action="toggle-tema"]). Em style.css, os valores que
  viviam em html[data-theme="dark"] viraram os únicos valores do :root
  (bloco de tema claro removido); os poucos seletores
  html[data-theme="dark"] que só ajustavam a sombra do .app-frame em
  preview mobile/desktop viraram regras incondicionais. Dashboard,
  Estoque de Insumos/Produção, #authView, dash-hero, dash-resumo, pastas
  do estoque e o dock continuam com paleta de marca fixa, como já era —
  não dependiam do toggle, não foram tocados. ATENÇÃO pra quem for mexer
  em comentários de style.css: um comentário que descrevia
  "--shadow-raised-*/--shadow-pressed-*" chegou a fechar o bloco CSS de
  comentário cedo demais (a sequência "*/" no meio do texto conta como
  fechamento) e quebrou o :root inteiro sem erro visível no console —
  vale sempre conferir `getComputedStyle(document.documentElement).
  getPropertyValue('--bg')` no console do navegador depois de editar
  comentários perto do topo do arquivo. Verificado com Playwright
  headless (script descartável, não ficou no repo): dashboard, "mais"
  (sem buraco onde era o switch), estoque, preview desktop e #authView,
  sem erros de console e sem atributo data-theme sobrando.

===========================================
FEEDBACK NOVO (04/09/2026) — cores brancas/creme, quer marrom
===========================================

- [RESOLVIDO em 08/09/2026, parcial — ver "NÃO coberto" no fim] Usuária
  não gostou do botão, da barra de pesquisa e da seta de voltar
  aparecendo brancos/creme — quer marrom. Achado no código: esses três
  elementos (seta de voltar = .iconbtn, barra de busca = .pressed.field,
  e botões tipo "Salvar ajustes como novo padrão" = .btn-ghost) usam
  background:var(--bg) de propósito — é o token neumórfico de base.

  Decisões confirmadas com a usuária: (1) marrom = hex fixo #442D1C,
  não var(--primary) — que seria outra cor se algum dia voltar a existir
  mais de um tema; (2) tema: deixou de fazer sentido depois que o modo
  claro foi removido do app inteiro na mesma sessão (decisão da usuária,
  não só desta pendência — ver seção própria); (3) escopo: só seta de
  voltar + busca, não os .iconbtn de "+" nem os .pressed.field de
  formulário comuns.

  Implementado em style.css (ver ".iconbtn[data-goto]" e
  ".pressed.field.row"): [data-goto] isola só a seta de voltar (os "+"
  usam [data-action], ficam de fora); .row isola só os 2 campos de busca
  reais — #estoqueSearchInput/#clientesSearchInput. box-shadow não
  precisou de recálculo (var(--shadow-raised-xs)/var(--shadow-pressed-sm)
  já são um drop-shadow fixo, sem depender de --bg pra combinar, desde a
  remoção do modo claro) — só border-color e color/placeholder, que
  --line/--surface-text*/--text-faint recalculam pra combinar com o
  fundo creme fixo do Dashboard/Estoque de Insumos/Produção (senão o
  ícone/texto ficariam escuros sobre um fundo agora também escuro
  nessas duas telas). Removido também o `style="color:var(--text-faint)"`
  inline do ícone de lupa nos 2 campos de busca (index.html), que tinha
  prioridade sobre qualquer regra do style.css — agora o ícone herda a
  cor do próprio .pressed.field.row. Verificado com Playwright headless
  em estoqueInsumos (onde o bug aparecia, tela de paleta fixa) e em
  clientes (tela normal) — sem regressão, sem erros de console.

  [RESOLVIDO em 08/09/2026] .btn-ghost ("Salvar ajustes como novo
  padrão" e outros do mesmo tipo): decisão da usuária foi TODOS os
  .btn-ghost do app, não só alguns — mesmo padrão do .iconbtn[data-goto]
  acima. Implementado em style.css (buscar por ".btn-ghost{"):
  background/color trocaram de var(--bg)/var(--text) pra hex fixo
  #442D1C/#F3E7D6. Nota pra próxima sessão: na conferência do código
  antes desse fix, nenhum .btn-ghost estático do index.html caía de
  fato dentro de Dashboard/Estoque de Insumos/Produção (as 3 telas de
  paleta fixa) — ou seja, a mudança não alterou nada visível nessas
  telas agora, é blindagem pra qualquer tela de paleta fixa atual ou
  futura. Se algum .btn-ghost aparecer errado depois disso, o bug está
  em outro lugar (provavelmente algo gerado por JS fora do escopo desta
  varredura), não neste CSS.

- Percentual "perda X% inclusa" na calculadora de velas (e na
  calculadora de lucro): usuária não entendeu o objetivo — já
  respondido no chat, mas registrando aqui pra próxima sessão não
  repetir a explicação. É a "perda de derretimento" (configState.
  perdaDerretimento, padrão 4%, editável em Configurações de produção
  via #cfgPerdaDerretimento) — infla a quantidade de insumo necessária
  pra cada item que "perde" no derretimento (item.perde), porque na
  prática se perde um pouco de cera/material no processo. Sem isso, a
  lista "insumos que serão usados" mostraria só a conta simples
  (quantidade × receita padrão), que fica menor do que o insumo real
  gasto na produção. Avaliar se vale deixar mais claro na UI (ex.:
  tooltip explicando "perda de derretimento" em vez de só a nota
  "perda 4% inclusa").

===========================================
REFERÊNCIAS NO CÓDIGO
===========================================

- app.js: painel de notificação e disparo automático — buscar por
  "Notificação do aviso de reposição"
- index.html: card "notificar por" — buscar por "notificar por"
- Supabase: projeto ponto-paragrafo (id vmqpvlcckcfixsryvfdn),
  Edge Function "enviar-aviso", tabela "estado" (1 linha por usuário,
  usada pela sincronização entre aparelhos desde 08/09/2026 — ver
  "Sincronização entre aparelhos — módulo 20" em app.js), Auth (login
  real desde 08/09/2026 — ver "Conta / sessão — módulo 9")
- index.html: nenhum atalho "em breve" restante — o último era o card
  "relatório completo" do dashboard, resolvido em 08/09/2026 (agora
  data-goto="relatorioCompleto")
- index.html: tela "mais" — buscar por data-screen="mais"
- app.js: leitura/cálculo de vendas sem tela de cadastro — buscar por
  "defaultVendasState"
- style.css: token de fundo branco/creme usado por seta de voltar, busca
  e botão ghost — buscar por ".iconbtn{", ".pressed{" e ".btn-ghost{"
  (base ainda é background:var(--bg)). Seta de voltar, busca e TODOS os
  .btn-ghost já têm override pra marrom fixo #442D1C — buscar por
  ".iconbtn[data-goto]", ".pressed.field.row" e ".btn-ghost{" (ver
  pendência "cores brancas/creme, quer marrom", já resolvida).
- app.js: perda de derretimento (o "% inclusa" da calculadora) — buscar
  por "perdaDerretimento"
- app.js: fluxo de Encomendas (criar/editar, avançar status, conversão
  em venda ao entregar) — buscar por "Encomendas — módulo 15"
- app.js: lista de Histórico de Vendas (busca, filtro de período,
  indicadores) — buscar por "Histórico de Vendas — módulo 16"
- app.js: Controle de Custos (custo real por vela, margem por
  variação, lista editável de custos fixos) — buscar por "Controle de
  Custos — tela — módulo 18"
- app.js: Relatório completo (vendido por variação, por forma de
  pagamento, cupons usados) — buscar por "Relatório completo — módulo 19"
- app.js: login real/sessão (Supabase Auth) — buscar por "Conta / sessão
  — módulo 9"; sincronização entre aparelhos (push/pull do blob de
  dados) — buscar por "Sincronização entre aparelhos — módulo 20"
